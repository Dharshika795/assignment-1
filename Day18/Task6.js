//Use prompt() to ask the user’s age — show a message if they can drive or not
let age=16;
if (age >= 18) {
  console.log("You can drive!");
} 
else {
  console.log("You are not old enough to drive.");
}
