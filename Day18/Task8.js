//Create a script that checks if a password is “secure” (≥ 8 characters).
let password = "mypassword123";
if (password.length >= 8) {
  console.log("Password is secure ");
} 
else {
  console.log("Password is too short ");
}
